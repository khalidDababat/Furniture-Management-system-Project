# Furniture Management System

All project code lives in `my-app/` (Next.js storefront + JSON Server data layer). Read `my-app/AGENTS.md` before working — it covers commands, architecture, i18n, and conventions.